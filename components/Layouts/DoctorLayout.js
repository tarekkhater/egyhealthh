import { useAuth } from '@/Hooks/doctorAuth '
import Head from 'next/head'
import DoctorNav from '../Navbars/DoctorNav'
import styles from '../../styles/Doctor/Home.module.css'






export default function DoctorLayout({container}) {
    const {user} = useAuth({'middleware':"auth" , 'redirectIfAuthenticated': ''})
    return (
        <div style={{width:'100%' , maxWidth:'100%'}}>
            <Head>
            <title>  EgyHealth ~ Doctor</title>
            <meta name="description" content="Generated by create next app" />
            <meta name="viewport" content="width=device-width, initial-scale=1" />
            <link rel="icon" href="/favicon.ico" />
            </Head>
            <header className={styles.header}>
                <DoctorNav doctor={user} />
            </header>
            <main className={styles.main}>
                {container}
            </main>
            <footer className={styles.footer}>
                <p>Copyright © 2022 egyhealth</p>
            </footer>
        </div>
    )
}
